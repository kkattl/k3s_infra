- name: reload sudoers
  ansible.builtin.command: /usr/bin/true
  notify: restart sshd   

- name: restart sshd
  ansible.builtin.systemd:
    name: ssh
    state: restarted
    daemon_reload: yes      
